# 🏫 Class Match (클래스 매치) - 학교 학급 보강 매칭 시스템

> **시험 기간 전 특정 학급의 보강이 필요한 교사와, 해당 학급의 수업을 빌려줄 수 있는 교사를 연결해 주는 Google Apps Script(GAS) 웹 애플리케이션**

---

## 🌟 핵심 기능 및 동작 방식 (Key Features & Workflow)

### 1. 보강 필요 교사의 등록 항목
보강이 필요한 교사가 다음 형식으로 입력하여 등록합니다:
- **희망 학급**: 복수 선택 가능 (학년 및 반 다중 선택 지원)
- **교과명**: 텍스트 창에 직접 입력 (예: `수학`, `영어`)
- **교사명**: 텍스트 창에 직접 입력 (예: `김수학`)
- **내선번호**: 텍스트 창에 직접 입력 (예: `302`)
- **총 희망 차수**: 1~10차시 드랍다운 메뉴로 선택
- **가능 요일 및 각 요일 별 가능 교시**: 월, 화, 수, 목, 금 요일별로 1교시~7교시 버튼을 토글하여 다중 선택할 수 있는 스마트 스케줄 매트릭스 UI 탑재 (예: `월(3, 5교시) | 수(2교시) | 금(4교시)` 형태로 자동 포맷팅)
- **비고/메모**: 시험 범위 또는 진도 안내 텍스트 입력

### 2. 학급마다 하나의 행으로 테이블 표시 & 열 별 필터 검색 (21-홍익_보강 지원 표준)
- 총 희망 차수가 3차시여도 하나의 학급이면 테이블 및 Google Sheets에 **학급마다 1개의 행**으로 깔끔하게 표시됩니다.
- 복수의 학급(예: 2학년 3반, 2학년 4반)을 선택하여 등록하면 각 학급별로 1행씩 등록됩니다.
- 테이블 상단 헤더 각 열마다 **상태, 희망 학급, 교과명, 희망 교사, 내선번호, 총 차수, 가능 요일/교시, 매칭 정보, 비고 메모** 별 필터 검색창 및 **[초기화]** 버튼이 제공되어 신속하게 원하는 보강 대상을 검색할 수 있습니다.

### 3. 보강 희망 수정 기능 (총 차수, 가능 요일/교시, 메모 변경)
- 보강을 올린 교사가 테이블의 **`[✏️ 수정]`** 버튼을 클릭하여 언제든지 **총 희망 차수**, **가능 요일 및 가능 교시**, **내선번호**, **비고/메모**, **진행 상태**를 편리하게 수정할 수 있습니다.

### 4. 개별 연락 및 매칭 성사 확인 워크플로우
1. **수업 대여 가능 교사**:
   - 테이블에서 학급, 과목, 총 차수, 가능 요일/교시, 내선번호를 확인
   - [📞 내선번호] 또는 [💬 메신저 텍스트]를 확인하여 보강 희망 교사에게 개별 연락
2. **보강을 올렸던 교사**:
   - 동료 교사와 수업 교환 협의가 완료되면, 해당 학급 행의 **`[🤝 매칭 완료]`** 버튼 클릭 (또는 `[✏️ 수정]`을 통해 총 차수 조율)
   - 수업을 빌려준 교사 정보를 입력(선택) 후 확정하면 상태가 `✅ 매칭완료`로 변경되어 동료 교사들에게 매칭되었음을 알림 (필요 시 `[재대기]`로 원복 가능)

---

## 📁 프로젝트 파일 구조

```text
24-class_match/
├── .agents/                 # 프로젝트 규칙 및 스킬 정의
│   ├── rules/
│   │   └── rules.md
│   └── skills/
│       ├── ask/
│       ├── git-commit/
│       └── scratchpad/
├── AGENTS.md                # 최상위 규칙 파일
├── appsscript.json          # Google Apps Script 매니페스트 (서울 표준시 Asia/Seoul, V8 런타임)
├── Code.gs                  # Google Apps Script 백엔드 로직 (차수별 행 자동 생성, 매칭 API)
├── Index.html               # 단일 웹 애플리케이션 프론트엔드 (Pretendard 폰트, 반응형 테이블 UI)
└── README.md                # 프로젝트 안내 문서
```

---

## 🚀 Google Apps Script 배포 방법 (Deployment Guide)

1. [Google Apps Script 콘솔](https://script.google.com/) 접속 후 새 프로젝트 생성
2. 좌측 파일 목록에 다음 파일들을 복사하여 붙여넣기:
   - `Code.gs`
   - `Index.html` (파일 추가 [+] -> HTML 선택 후 이름으로 `Index` 또는 `index` 입력 후 내용 붙여넣기)
   - `appsscript.json` (프로젝트 설정 -> "appsscript.json 매니페스트 파일 표시" 체크 후 수정)
3. 우측 상단 **[배포]** -> **[새 배포]** 클릭
4. 유형 선택: **웹 앱(Web App)**
   - 설명: `Class Match v1.1`
   - 다음 사용자로 실행: `나(내 계정)`
   - 액세스 권한이 있는 사용자: `모든 사용자(Anyone)` 또는 `조직 내 사용자`
5. **[배포]** 버튼 클릭 후 생성된 **웹 앱 URL**을 교사들과 공유하여 사용

---

## 📝 변경 이력 (Changelog)

### [2026-09-17 20:56] 업데이트 이력 (Commit ID: 088eb4c)
- **수정 내용**:
  1. **상단 요약 통계 카드 배너 삭제**: 전체 보강 대상 학급 수, 대기중, 매칭 성사 완료 카드 영역(`.stats-row`) 완전 제거.
  2. **상단 탭 바 제거 및 단일 테이블 화면 직결**: 탭 전환 버튼 바(`.tabs-bar`) 제거, 메인 테이블 카드 상단 툴바에 `[+ 보강 희망 등록]`, `[⚙ 시트 설정]` 모달 호출 버튼 배치.
  3. **열(Column) 별 필터 검색 기능 구현 (`21-홍익_보강 지원` 프로젝트 표준)**: 상태, 희망 학급, 교과명, 희망 교사, 내선번호, 총 차수, 가능 요일/교시, 매칭 정보, 비고 메모 열별 필터 input/select 및 `[🔄 초기화]` 버튼(역회전 애니메이션) 탑재.
  4. **시스템 전역 '요청' ➔ '희망' 텍스트 변경**: `Index.html`, `Code.gs`, `README.md` 내 '보강 요청', '요청 교사', '요청 등록' 등의 표현을 '보강 희망', '희망 교사', '희망 등록' 등으로 전면 치환.
- **검증 결과**:
  - `Node.js vm.Script` AST 문법 검사: `Code.gs` 및 `Index.html` 내 인라인 자바스크립트 구문 이상 없음 (`Syntax OK`).
  - 터미널 기반 Git 워킹 트리 및 무결성 검증 완료.
